#include <stdio.h>

/**
 * main - function that prints values between 0 and 50
 * Return: (0) Always
 */

int main(void)
{
    int i = 1;
    
    while (i < 50)
    {
	printf("%d\n", i);
	 i++;
    }

     return 0;
}
